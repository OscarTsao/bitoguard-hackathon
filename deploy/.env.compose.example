# For local Docker and EC2 Docker Compose deployments.
# Only change BITOGUARD_SOURCE_URL if you need to re-run the sync pipeline.

BITOGUARD_SOURCE_URL=http://mock-api:8000
BITOGUARD_LABEL_SOURCE=hidden_suspicious_label

